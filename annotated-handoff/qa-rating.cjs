const {chromium}=require('/Users/RiRosen/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('assert/strict');(async()=>{const b=await chromium.launch({headless:true,channel:'chrome'});const p=await b.newPage();await p.goto('http://localhost:8100');for(let i=0;i<5;i++)await p.locator('[data-nav="home"]').click();assert(!await p.locator('.rating-modal').isVisible());await p.locator('[data-nav="home"]').click();await p.locator('[data-rating="4"]').click();assert.equal(await p.locator('.rating-stars .selected').count(),4);await p.waitForTimeout(500);assert(!await p.locator('.rating-modal').isVisible());await p.reload();for(let i=0;i<8;i++)await p.locator('[data-nav="home"]').click();assert(!await p.locator('.rating-modal').isVisible());await p.evaluate(()=>sessionStorage.clear());await p.reload();for(let i=0;i<6;i++)await p.locator('[data-nav="home"]').click();await p.locator('[data-action="close-rating"]').click();assert(!await p.locator('.rating-modal').isVisible());console.log('PASS six-click trigger, unobstructed stars, selection, close and session persistence');await b.close()})().catch(e=>{console.error(e);process.exit(1)});
